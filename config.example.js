// This file shows the shape config.js needs. The deploy workflow generates
// the real config.js automatically from the GROQ_API_KEY repo secret —
// you never write it by hand and it is never committed.
//
// For local testing only: copy this file to config.js (already git-ignored)
// and paste in your own key from https://console.groq.com/keys
window.GROQ_KEY = "";
